version: "3"
services:
  #Servidor PHP
  php:
    build: ./docker/php
    container_name: php
    volumes:
      - ./app:/var/www/html
    links:
      - db
    networks:
      - profiti-network
  #Servidor Web APACHE
  web:
    build: ./docker/apache
    container_name: apache
    volumes:
      - ./app:/var/www/html
    ports:
      - "3000:80" #Porta a ser acessada via Localhost
    links:
      - php
    networks:
      - profiti-network
  #Servidor Banco de Dados Maria DB
  db:
    image: mariadb
    container_name: db
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: profiti
     #MYSQL_USER: root
      MYSQL_PASSWORD: profiti
      MYSQL_ROOT_PASSWORD: profiti
    volumes:
      - ./docker/database:/var/lib/mysql
    networks:
      - profiti-network

  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: phpmyadmin
    restart: always
    links:
      - db
    ports:
      - 8181:80
    environment:
      MYSQL_USERNAME: root
      MYSQL_ROOT_PASSWORD: profiti
      PMA_ARBITRARY: 1
    networks:
      - profiti-network
networks:
  profiti-network:
    driver: bridge